spring.application.name=appointment
server.port=8084


api.security.token.secret=c8d1e3f7g9h2j4k6l8m0n5p9q7r5s3t1u0v8w6x4y2z0a1b3c5d7e9f
# Deve ser EXATAMENTE o que está no .setIssuer() do auth service
spring.security.oauth2.resourceserver.jwt.issuer-uri=auth

#------------------------------------------------------------------
# A chave deve ser a mesma usada no servi�o de 'login' para assinar o 'token'
spring.security.oauth2.resource.server.jwt.issuer-uri=http://localhost:/8081
# OU se voc� usa chave sim�trica (HS256):
jwt.secret=c8d1e3f7g9h2j4k6l8m0n5p9q7r5s3t1u0v8w6x4y2z0a1b3c5d7e9f

#-------------------------------------------------------------------
#Servi�os para conex�o ass�ncrona
entity.service.url=http://localhost:8082
user.service.url=http://localhost:8081
#-------------------------------------------------------------------

#-------------------------------------------------------------------
#Configura��es AWS e chaves de Acesso
spring.cloud.aws.region.static=sa-east-1
spring.cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
spring.cloud.aws.credentials.secret-key=${AWS_SECRET_ACCESS_KEY}
medcare.aws.sqs.queue.notification.email=NotificationEmailQueue
#-------------------------------------------------------------------


# ------------------------------------------
# Configuracoes do email
# ------------------------------------------
broker.queue.email.name=default.email

# ----------------------------------------
# 4. Configuracao do MySQL (ms_profile)
# ----------------------------------------
#  spring.datasource.url=jdbc:mysql://localhost:3306/ms_appointment
#  spring.datasource.username=root
#  spring.datasource.password=admin
#  spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # # Configura��es do JPA/Hibernate
#  spring.jpa.hibernate.ddl-auto=update
#  spring.jpa.show-sql=true
#  spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ----------------------------------------
# 5. Configuração da Conex�o H2 (URL, Driver, Dialect)
# ----------------------------------------

# Tipo de conexão: in-memory (dados perdidos ao reiniciar)
spring.datasource.url=jdbc:h2:mem:testdb
# Nome de usuario e senha (opcional para in-memory, mas bom para pr�tica)
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.driverClassName=org.h2.Driver

# Habilita o Hibernate a usar o dialeto H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# ----------------------------------------
# 6. Configuração da Inicialização e Console
# ----------------------------------------

# Cria automaticamente as tabelas (create/update/none)
spring.jpa.hibernate.ddl-auto=update

# Habilita o console do H2 (Interface Web para visualizar os dados)
spring.h2.console.enabled=true

# Define o caminho de acesso ao console (ex: http://localhost:8082/h2-console)
#spring.h2.console.path=/h2-console